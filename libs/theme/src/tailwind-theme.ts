import type { Config } from 'tailwindcss';

const tailwindPreset: Partial<Config> = {
  theme: {
    colors: {
      kek: {
        500: '#ff0000',
      },
    },
  },
  plugins: [],
};

export default tailwindPreset;
